name: Apply v1.5.50 once

on:
  push:
    paths:
      - '.github/workflows/apply-v1550.yml'

permissions:
  contents: write

jobs:
  apply-v1550:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply v1.5.50 patch
        shell: bash
        run: |
          cat > /tmp/v1550.patch <<'PATCH'
--- a/lobster_discord.py
+++ b/lobster_discord.py
@@ -10,7 +10,7 @@
 import traceback
 from PIL import Image, ImageDraw, ImageFont, ImageOps
 
-LOBSTER_VERSION = "1.5.48"
+LOBSTER_VERSION = "1.5.50"
 
 
 def _normalize_generation_level(level):
@@ -14825,6 +14825,102 @@
     return text
 
 
+def _is_face_closeup_request_text(text):
+    raw = str(text or "")
+    low = raw.lower()
+    zh_keys = [
+        "臉部特寫", "脸部特写", "臉部美容特寫", "脸部美容特写", "近拍臉部", "近拍脸部",
+        "臉部近拍", "脸部近拍", "臉部大特寫", "脸部大特写", "鎖骨以上", "锁骨以上",
+        "肩膀以上", "肩膀以上", "頭肩特寫", "头肩特写", "頭像照", "头像照",
+    ]
+    en_keys = [
+        "face close-up", "face closeup", "beauty close-up", "beauty closeup", "close-up beauty portrait",
+        "closeup beauty portrait", "headshot", "head shot", "collarbone-up", "shoulders-up", "shoulder-up",
+        "chest-up only", "face fills most of the frame", "face beauty close-up",
+    ]
+    return any(k in raw for k in zh_keys) or any(k in low for k in en_keys)
+
+
+def _seedream_face_closeup_prompt(custom_prompt, has_reference=False, current_outfit=None, visual_checklist=None):
+    """v1.5.49: special prompt for face close-ups only. Deliberately avoids body-shape global traits."""
+    raw_prompt = str(custom_prompt or "")
+    camera_override = ""
+    marker = "[[CAMERA_OVERRIDE]]"
+    if marker in raw_prompt:
+        raw_prompt, camera_override = raw_prompt.rsplit(marker, 1)
+        camera_override = _clean_text_compact(camera_override)[:420]
+    cleaned_request = _v1527_strip_prompt_conflicts(raw_prompt)
+    hard = _v1527_scene_hard_requirements(cleaned_request or custom_prompt)
+    people = _v1527_concise_people_policy(visual_checklist)
+
+    sections = [
+        "FIGURE ROLE MAP — obey these roles strictly.",
+        (
+            "Figures 1-9 are identity-only references for Xiaoxia. Apply their face identity, hairstyle identity, and overall recognizable adult East Asian appearance only to Xiaoxia. "
+            "Do not copy their pose, outfit, background, lighting, or composition."
+        ),
+        "FACE CLOSE-UP MODE — this request is a beauty close-up, not a body shot.",
+        (
+            "Mandatory framing: collarbone-up or shoulders-up only. Xiaoxia's face must occupy most of the frame. "
+            "Show only the visible neckline / shoulder area of the outfit if naturally present. "
+            "Do not show waist, hips, thighs, knees, legs, or a full-body composition."
+        ),
+        (
+            "Beauty focus: emphasize Xiaoxia's eyes, skin texture, lips, hair, expression, and subtle facial contour. "
+            "Use flattering natural light and let the background remain secondary and softly unobtrusive."
+        ),
+    ]
+    if hard:
+        sections.append("HARD SCENE REQUIREMENTS — failure on any item is incorrect:\n" + hard)
+    sections.append("PEOPLE POLICY:\n" + people)
+
+    if has_reference:
+        sections.append(
+            "FIGURE 10 WARDROBE ROLE:\n"
+            "- Figure 10 controls the visible clothing neckline / shoulder details and visible fashion accessories only.\n"
+            "- Preserve its color, material, pattern, and key visible details if they appear inside the close-up crop.\n"
+            "- Figure 10 must not control scene, pose, background, camera angle, or composition."
+        )
+    elif current_outfit:
+        sections.append(
+            "VISIBLE OUTFIT CONTINUITY:\n"
+            f"- {str(current_outfit).strip()}\n"
+            "- Only preserve the visible neckline / shoulder styling that can naturally appear within a face close-up crop."
+        )
+    else:
+        sections.append("OUTFIT ROLE:\n- No Figure 10 is supplied; keep only simple, natural visible neckline styling appropriate to the scene.")
+
+    sections.append("TEXT REQUEST — highest authority for scene, time, action, pose, and composition:\n" + cleaned_request)
+    sections.append(
+        "QUALITY:\n"
+        "- Photorealistic beauty portrait, natural anatomy and hands, especially around the face and neckline.\n"
+        "- This is a face-focused image, not a fashion full-body image.\n"
+        "- Do not zoom out to show torso, waist, hips, or legs just to satisfy styling continuity."
+    )
+    final = "\n\n".join(s for s in sections if str(s).strip()).strip()
+    if camera_override:
+        final += "\n\nCAMERA OVERRIDE — this framing is mandatory:\n" + camera_override
+    print(
+        f"🧭 [FACE_CLOSEUP_PROMPT_STATS_V1549] total_chars={len(final)} "
+        f"request_chars={len(cleaned_request)} hard_chars={len(hard)} people_chars={len(people)}"
+    )
+    return final
+
+
 def _v1527_concise_people_policy(checklist=None):
     c = checklist if isinstance(checklist, dict) else {}
     policy = str(c.get("people_policy") or "")
@@ -14850,7 +14946,16 @@
 
 
 def _seedream_photo_prompt(custom_prompt, has_reference=False, current_outfit=None, visual_checklist=None):
-    """v1.5.47: concise photo prompt with an optional final camera override for /寫真 More."""
+    """v1.5.49: concise photo prompt with a face-closeup special branch for /寫真 More and direct close-up requests."""
+    if _is_face_closeup_request_text(custom_prompt):
+        return _seedream_face_closeup_prompt(
+            custom_prompt,
+            has_reference=has_reference,
+            current_outfit=current_outfit,
+            visual_checklist=visual_checklist,
+        )
+
     raw_prompt = str(custom_prompt or "")
     camera_override = ""
     marker = "[[CAMERA_OVERRIDE]]"
@@ -16049,7 +16154,36 @@
 # ==========================================
 PHOTOBOOK_WARDROBE_PAGE_SIZE = 6
 PHOTOBOOK_WARDROBE_MAX_CANDIDATES = 30
-PHOTOBOOK_WARDROBE_PRIVATE_CATEGORIES = {"內衣", "泳裝", "睡衣／居家服", "Cosplay", "鞋子", "包包", "配件"}
+PHOTOBOOK_WARDROBE_ALWAYS_EXCLUDE_CATEGORIES = {"鞋子", "包包", "配件"}
+PHOTOBOOK_WARDROBE_USUALLY_PRIVATE_CATEGORIES = {"內衣", "泳裝", "睡衣／居家服"}
+PHOTOBOOK_WARDROBE_SCENE_PRIVATE_HINTS = [
+    "在家", "家中", "家裡", "臥室", "房間", "客廳", "沙發", "床邊", "床上", "私密", "只能在家中穿",
+    "居家", "晚安", "睡前", "夜晚", "晚上", "夜裡", "晨起", "剛睡醒"
+]
+PHOTOBOOK_WARDROBE_SCENE_PUBLIC_HINTS = [
+    "文創", "園區", "書店", "美術館", "公園", "戶外", "散步", "街", "白天", "午後", "華山", "松菸", "河濱",
+    "咖啡", "展覽", "校園", "巷弄", "百貨", "車站"
+]
+PHOTOBOOK_WARDROBE_SCENE_SOFT_SEXY_HINTS = [
+    "溫柔", "性感", "害羞", "微性感", "柔軟", "慵懶", "曖昧", "撒嬌", "親密", "女友感"
+]
+
+
+def _photobook_scene_profile(scene_text):
+    scene = str(scene_text or "").strip().lower()
+    private_home = any(k in scene for k in PHOTOBOOK_WARDROBE_SCENE_PRIVATE_HINTS)
+    public_outdoor = any(k in scene for k in PHOTOBOOK_WARDROBE_SCENE_PUBLIC_HINTS)
+    soft_sexy = any(k in scene for k in PHOTOBOOK_WARDROBE_SCENE_SOFT_SEXY_HINTS)
+    swim_related = any(k in scene for k in ["泳", "泳池", "海邊", "沙灘", "溫泉", "泡湯"])
+    formal_event = any(k in scene for k in ["宴會", "晚宴", "婚禮", "典禮", "記者會"])
+    return {
+        "scene": scene,
+        "private_home": private_home,
+        "public_outdoor": public_outdoor,
+        "soft_sexy": soft_sexy,
+        "swim_related": swim_related,
+        "formal_event": formal_event,
+    }
 
 
 def _photobook_wardrobe_scene_score(item, scene_text, recent_ids=None):
@@ -16060,29 +16194,51 @@
     if not ref_path:
         return -9999.0
     main = str(item.get("main_category") or "").strip()
-    if main in PHOTOBOOK_WARDROBE_PRIVATE_CATEGORIES:
+    if main in PHOTOBOOK_WARDROBE_ALWAYS_EXCLUDE_CATEGORIES:
         return -9999.0
+    if main == "Cosplay":
+        return -9999.0
+
     text = " ".join([
         str(item.get("name") or ""), main, str(item.get("sub_category") or ""),
         " ".join(str(x) for x in (item.get("tags") or [])), str(item.get("style_summary") or "")
     ]).lower()
-    scene = str(scene_text or "").lower()
     sem = _wardrobe_semantic_profile(item)
+    scene_profile = _photobook_scene_profile(scene_text)
     score = 10.0
-    # 公開白天場景偏好外出、休閒、旅遊、社交；不靠名稱單點判斷。
-    public_day = any(k in scene for k in ["文創", "園區", "書店", "美術館", "公園", "戶外", "散步", "街", "白天", "午後", "華山", "松菸", "河濱"])
-    if public_day:
+
+    # 公開白天場景：偏外出、休閒、旅遊、社交；私密服裝大幅扣分。
+    if scene_profile["public_outdoor"] and not scene_profile["private_home"]:
         if main in {"洋裝", "套裝", "上衣", "下身", "外套"}: score += 8
         if any(x in (sem.get("occasion") or []) for x in ["休閒", "社交", "旅遊"]): score += 7
         if any(x in (sem.get("style") or []) for x in ["知性", "休閒", "甜美", "優雅", "俐落"]): score += 4
-        if any(k in text for k in ["內衣", "胸罩", "lingerie", "睡衣", "睡袍", "泳裝", "比基尼"]): score -= 40
-    if any(k in scene for k in ["夏", "白天", "午後", "戶外"]):
+        if main in PHOTOBOOK_WARDROBE_USUALLY_PRIVATE_CATEGORIES: score -= 42
+        if any(k in text for k in ["內衣", "胸罩", "lingerie", "睡衣", "睡袍", "比基尼"]): score -= 24
+
+    # 居家私密場景：允許內衣／睡袍／睡衣／居家服進榜，反而優先這些衣服。
+    if scene_profile["private_home"]:
+        if main in {"內衣", "睡衣／居家服", "洋裝", "套裝", "上衣", "下身"}: score += 6
+        if main in {"內衣", "睡衣／居家服"}: score += 18
+        if any(x in (sem.get("occasion") or []) for x in ["內著", "睡眠", "居家"]): score += 12
+        if any(x in (sem.get("style") or []) for x in ["性感", "甜美", "優雅"]): score += 6
+        if scene_profile["soft_sexy"] and any(k in text for k in ["蕾絲", "透膚", "細肩帶", "吊帶", "睡袍", "睡裙", "睡衣", "晨袍", "絲質", "緞面", "柔軟", "居家"]):
+            score += 9
+        if any(k in text for k in ["商務", "辦公", "西裝", "職場", "制服", "套裝"]): score -= 18
+        if any(k in text for k in ["厚重", "大衣", "羽絨", "風衣"]): score -= 10
+        if main == "泳裝" and not scene_profile["swim_related"]: score -= 22
+
+    if any(k in scene_profile["scene"] for k in ["夏", "白天", "午後", "戶外"]):
         if "春夏" in (sem.get("season") or []): score += 4
         if any(k in text for k in ["短袖", "無袖", "輕薄", "棉麻", "雪紡"]): score += 2
-    if any(k in scene for k in ["文創", "書店", "美術館", "展覽"]):
+    if any(k in scene_profile["scene"] for k in ["文創", "書店", "美術館", "展覽"]):
         if any(k in text for k in ["襯衫", "學院", "知性", "文藝", "優雅", "簡約", "牛仔"]): score += 5
+    if scene_profile["formal_event"]:
+        if any(x in (sem.get("formality") or []) for x in ["輕正式", "禮服", "正式"]): score += 8
+        if any(x in (sem.get("style") or []) for x in ["優雅", "俐落"]): score += 3
+
     wid = str(item.get("id") or "").strip().upper()
-    if recent_ids and wid in recent_ids: score -= 5
+    if recent_ids and wid in recent_ids:
+        score -= 5
     # 少量穩定亂數，避免同分永遠固定；以 ID 為種子，同次排序仍穩定。
     score += (sum(ord(c) for c in wid) % 17) / 100.0
     return score
@@ -16179,6 +16335,11 @@
 async def _xiaoxia_review_photobook_wardrobe_page(grid_path, items, scene_text, page_no, total_pages):
     raw = await asyncio.to_thread(Path(grid_path).read_bytes)
     names = "\n".join(f"{i+1}. {it.get('id')} {it.get('name')}" for i, it in enumerate(items))
+    scene_profile = _photobook_scene_profile(scene_text)
+    if scene_profile.get("private_home"):
+        fit_rule = "請判斷實際外觀是否適合這個家中私密寫真場景，是否有溫柔、微性感、害羞或放鬆的女友感。內衣、睡袍、睡衣、居家服都可以，只要真的適合在家穿、畫面好看、能襯托她。若圖片其實太像正式外出、上班、晚宴或完全不符合『只能在家中穿』，要明確指出。"
+    else:
+        fit_rule = "請判斷實際外觀是否適合這個場景、是否可公開外穿、是否能襯托妳且適合多種寫真取景。若名稱像洋裝但圖片其實像內衣、睡衣或不適合公開場合，要明確指出。"
     prompt = f"""
 妳是小俠，正在和男友大俠一起為寫真挑衣服。請真正看六宮格候選圖，而不是只相信名稱。
 寫真情境：{scene_text}
@@ -16186,8 +16347,7 @@
 候選對照：
 {names}
 
-請判斷實際外觀是否適合這個場景、是否可公開外穿、是否能襯托妳且適合多種寫真取景。
-若名稱像洋裝但圖片其實像內衣、睡衣或不適合公開場合，要明確指出。
+{fit_rule}
 只回 JSON：
 {{"favorite":1,"second":2,"avoid":[3],"reply":"以小俠自然女友口吻，簡短說最喜歡哪件、其次哪件、哪些不建議與理由。不要提 Vision、模型、系統或 JSON。"}}
 """
@@ -17431,7 +17591,7 @@
     {"id": "eye_level_full", "name": "平視全身照", "prompt": "Eye-level full-body portrait. Keep head and feet inside the frame.", "axis": ("full", "front", "eye"), "needs": ()},
     {"id": "low_full", "name": "略低機位全身照", "prompt": "Slight low-angle full-body portrait. Keep the full body visible and natural.", "axis": ("full", "front", "low"), "needs": ("standing",)},
     {"id": "high_half", "name": "略高機位俯拍半身", "prompt": "Slight high-angle waist-up portrait. Do not use a full-body composition.", "axis": ("half", "front", "high"), "needs": ()},
-    {"id": "face_closeup", "name": "臉部美容特寫", "prompt": "CHEST-UP ONLY. Her face fills most of the frame. No full body or legs.", "axis": ("closeup", "front", "eye"), "needs": ()},
+    {"id": "face_closeup", "name": "臉部美容特寫", "prompt": "FACE BEAUTY CLOSE-UP ONLY. Frame from collarbone-up or shoulders-up only. Her face must fill most of the frame. Do not show waist, hips, thighs, knees, legs, or a full-body composition.", "axis": ("closeup", "front", "eye"), "needs": ()},
     {"id": "left45", "name": "左側 45° 取景", "prompt": "Move the camera to Xiaoxia's left-front 45-degree angle. Keep the scene coherent.", "axis": ("same", "left45", "eye"), "needs": ()},
     {"id": "right45", "name": "右側 45° 取景", "prompt": "Move the camera to Xiaoxia's right-front 45-degree angle. Keep the scene coherent.", "axis": ("same", "right45", "eye"), "needs": ()},
     {"id": "behind45", "name": "後側 45° 取景", "prompt": "Move the camera behind Xiaoxia at a 45-degree angle. Preserve the same moment.", "axis": ("same", "rear45", "eye"), "needs": ("rear_space",)},
@@ -17649,7 +17809,7 @@
 - 依照片中真正可見的取景判斷，不能因畫面漂亮就通過。
 - passed=true 代表目標拍攝手法清楚、可辨識地完成。
 - score 為 0.0～1.0 的符合程度。
-- 要求臉部特寫，若仍出現腰部以下、腿部或接近全身，passed=false。
+- 要求臉部特寫，若未收在鎖骨以上或肩膀以上，或仍出現腰部以下、腿部、明顯胸腰身材構圖、或接近全身，passed=false。
 - 要求全身照，若頭或腳明顯被裁掉，passed=false。
 - 要求左右45°，需看出明確方向變化，不是只改表情。
 - 要求背後／後側，需看出背部或明顯後側視角。
@@ -17713,13 +17873,28 @@
         role_note = "Figure 1 is the photograph to edit. Figures 2-10 are identity-only references for Xiaoxia."
 
     camera_recipe = str((shot or {}).get("prompt") or "Use a clearly different camera composition.").strip()
-    final_prompt = (
-        f"{role_note}\n"
-        "Create another photograph from the same photoshoot and the same moment. "
-        "Keep Xiaoxia, her current action, outfit, accessories, hairstyle, location and lighting naturally consistent with Figure 1. "
-        "Change the camera treatment only.\n"
-        f"CAMERA: {camera_recipe}"
-    )
+    if str((shot or {}).get("id") or "") == "face_closeup":
+        final_prompt = (
+            f"{role_note}\n"
+            "Create another photograph from the same photoshoot and the same moment. "
+            "Keep Xiaoxia's identity, hairstyle, visible outfit details near the neckline, location, and lighting naturally consistent with Figure 1. "
+            "This request is a face beauty close-up only. Crop from collarbone-up or shoulders-up only. "
+            "Her face must fill most of the frame. The composition must not show waist, hips, thighs, knees, legs, or a full-body view. "
+            "Background stays secondary and soft. Change the camera treatment only.\n"
+            f"CAMERA: {camera_recipe}"
+        )
+    else:
+        final_prompt = (
+            f"{role_note}\n"
+            "Create another photograph from the same photoshoot and the same moment. "
+            "Keep Xiaoxia, her current action, outfit, accessories, hairstyle, location and lighting naturally consistent with Figure 1. "
+            "Change the camera treatment only.\n"
+            f"CAMERA: {camera_recipe}"
+        )
     trace = {
         "kind": "photobook_more",
         "trace_id": _new_generation_trace_id("photobook_more"),
@@ -17783,11 +17958,19 @@
     )
     camera_recipe = str((shot or {}).get("prompt") or "Use a clearly different camera composition.").strip()
     context["camera_override"] = camera_recipe
-    context["prompt_base"] = (
-        root_prompt
-        + "\n[[CAMERA_OVERRIDE]] Same Xiaoxia photoshoot and same location. "
-        + camera_recipe
-    )
+    if str((shot or {}).get("id") or "") == "face_closeup":
+        context["prompt_base"] = (
+            root_prompt
+            + "\n[[CAMERA_OVERRIDE]] Same Xiaoxia photoshoot and same location. FACE BEAUTY CLOSE-UP ONLY. "
+            + camera_recipe
+            + " Keep the image collarbone-up or shoulders-up only. Her face fills most of the frame. Do not show waist, hips, thighs, knees, legs, or a full-body composition."
+        )
+    else:
+        context["prompt_base"] = (
+            root_prompt
+            + "\n[[CAMERA_OVERRIDE]] Same Xiaoxia photoshoot and same location. "
+            + camera_recipe
+        )
     context["trace_action"] = "photobook_more_generate_fallback"
     context["__trace_context"] = {
         "kind": "photobook_more",
PATCH
          git apply --check /tmp/v1550.patch
          git apply /tmp/v1550.patch
          python -m py_compile lobster_discord.py
      - name: Commit replacement
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lobster_discord.py
          git commit -m "Update lobster_discord to v1.5.50"
          git push
